# Dustline 单机辅助（BepInEx 插件）

给 Dustline 0.6.6 加的单机辅助：**透视 ESP + 自瞄（带范围圈）+ 穿墙 + 无后座 + 无散射**。

> **子弹追踪已删除**（原因见文末"为什么删掉子弹追踪"）。

---

## 怎么用

1. **双击 `D:\cs\Dustline.exe`**
2. 主菜单点 **「开始单人训练」**
3. 游戏里按 **`Insert`** 开/关设置窗口

设置存在 `BepInEx\config\dsh.dustline.assist.cfg`（改完重启游戏生效）。

---

## 这一版修了什么

### ① 范围圈和实际锁定范围不一致 —— 找到了，是 2 倍差

**原因**：锁定判定用的是 `Vector3.Angle < AimFov`，也就是说 **`AimFov` 是"从准心量起的半径角"**；
但我画圈时错用了 **`AimFov / 2`**：

```csharp
float half = AimFov.Value * 0.5f;      // ← 错：圈只画了实际范围的一半
```

所以**实际锁定范围正好是圈的 2 倍**，和你说的"实际比圈大很多"完全对上。

**修复**：两边统一用 `AimFov`。并且用**数值校验**确认（不是靠眼看）：

```
夹角=26.2°  屏幕距中心=480px  圈半径=454px  | 屏幕=1440x1080 FOV=58
```

独立验算：焦距 = 540/tan(29°) = 974px

| 角度 | 公式算 | 实测 |
|---|---|---|
| 26.2° | 974 × tan(26.2°) = 479px | **480px** ✓ |
| 25°（圈） | 974 × tan(25°) = 454px | **454px** ✓ |

**圈和锁定范围现在是同一个角度了。**

顺带把默认 `Fov` 从 40 改成 **25**（1080p 下圈半径约 454px，能看见、也合理）。
觉得吸得太广就调小，想更宽松就调大。

### ② 自瞄不够准（打高了 / 隔墙半天打不死）—— 修了两处

**第一处（真 bug）**：`ApplyNoRecoil()` 原来写在 **`LateUpdate`** 里 ——
那时游戏**已经读完输入、生成完子弹指令了**，所以**当前这一枪照样吃后坐力**。

现在挪进 **`Game.Update` 的前缀**，和自瞄一起、在生成指令**之前**执行：

```csharp
static void UpdatePrefix()
{
    Inst.ApplyNoRecoil();   // 先清后坐力
    Inst.RunAimbot();       // 再瞄
}
```

**第二处**：瞄准原点原来用的是**相机位置**，而子弹是从**玩家眼睛**射出的。
两者位置不同会让角度有偏差。现在统一用**本地玩家的 `Eye`**。

**另外加了一个微调项** `HeadOffset`（米）：

- 总是打**高**一点 → 填负数（如 `-0.1`）
- 总是打**低**一点 → 填正数

> 说明：**隔墙打得慢是游戏机制** —— CS 的子弹穿墙后伤害按穿透层数和距离衰减，
> 厚墙后的目标本来就要多打几枪。如果配合穿墙用，这个是正常的。

### ③ 穿墙 —— 你说生效了，保留不变

三层挂钩：`BulletPenetration.Remaining` + `BallisticSurface` 穿透系数 + `Match.TraceBullet` 拉满武器穿透力。

### ④ 左键带自瞄 —— 已根治

**原因**：子弹追踪原来挂在左键上（`Input.GetMouseButton(0)`），而它靠"改视角"实现，所以一开火就吸。

**现在**：子弹追踪已删除，**左键只负责开火**。自瞄只认 **`Mouse1`（鼠标右键）**。

---

## 功能开关（游戏里 Insert）

| 分组 | 选项 | 说明 |
|---|---|---|
| **透视 ESP** | 总开关 / 方框 / 汇聚线 / 血量 / 距离 / BOT 标记 | 敌人红框 + 屏幕上方拉的线 |
| **自瞄** | 自瞄总开关 | 按住右键才生效 |
| | 画出锁定的目标 | 目标上画绿十字 |
| | 显示自瞄范围圈 | 屏幕上那个虚线圆 |
| | （范围 / 转速 / 准心偏移） | 配置文件：`Fov` / `Speed` / `HeadOffset` |
| **枪械** | 无后座 | 清零后坐力 + 累积 + 精度惩罚 |
| | 无散射 | 子弹完全走准心 |
| | 穿墙 | 多厚的墙都能穿 |
| **调试** | 输出日志 / 自动开火 / 自动开局 | 排查用 |

**圈的颜色含义**：

| 颜色 | 含义 |
|---|---|
| 青色 | 待命（没按右键） |
| 黄色 | 按住右键了，但圈内没敌人 |
| **绿色** | **已锁定** |

---

## ⚠️ 联机保护

所有功能只在**离线对局**生效（读 `Game.Offline`）。联机时窗口顶部显示
`状态：非离线（联机/菜单）— 辅助已禁用`，全部自动关闭。

---

## 怎么验证

| 要测什么 | 怎么测 |
|---|---|
| **透视** | 敌人身上有没有红框（隔墙也有） |
| **范围圈** | 青色虚线圆；按住右键变黄/变绿 |
| **圈是否准** | 勾上"输出调试日志"，日志会打 `圈校验: 夹角=.. 屏幕距=.. 圈半径=..`，三者应自洽 |
| **自瞄** | 按住**右键**，只有**圈内**敌人才被吸；松开自由移动 |
| **无散射** | 对墙连射，弹孔集中一点 |
| **无后座** | 连射时准心不往上飘 |
| **穿墙** | 隔任意厚度墙打敌人 |

---

## 为什么删掉子弹追踪

你要的是"**无论从哪里开火都能爆头，比如朝天开枪敌方也会死**"。我做了，但**实测无效**，有日志证据：

```
子弹方向(数组) 长度=16  改写前[0]=(-0.887, 0.000, -0.462)  目标=(0.252, -0.074, 0.965)
子弹方向(数组) 长度=16  改写前[0]=(0.180, -0.151, -0.972)  目标=(0.209, -0.138, -0.968)
```

分析：

1. **`ShotDirection()`（单个方向的版本）从头到尾没被调用过**，计数恒为 0
2. 真正被调用的是 `ShotDirections()`，但它填的是一个**固定 16 元的弹道模板表**，
   改写它**不影响子弹实际飞行方向**（第二行的"改写前"≈第一行的"目标"，
   说明我的改写确实写进数组了，但游戏不拿这个数组当每发方向）
3. **子弹的真实方向来自 `Command.Yaw/Pitch`（视角）**，而 `Command` 是**结构体**，
   Harmony 前缀收到的是副本，改了写不回去

**唯一能改弹道的办法是改视角** —— 但那正是导致"一按左键就吸过去"的原因，是你明确不要的。

所以按你说的**整条移除**（配置项和 UI 都删了，源码里留了注释说明）。

> 想要"自动爆头"的效果，现在的组合已经够用：
> **按住右键（自瞄吸头）→ 左键开火 → 无散射保证不飘**。

---

## 技术细节

### 环境
- Dustline 0.6.6，**Unity 6（6000.3.23）Mono**
- 加载器：**BepInEx 5.4.23.5**（x64）

### 源码
| 文件 | 说明 |
|---|---|
| `Assist.cs` | 插件本体（主源码） |
| `Dump.cs` | 反射 dump 工具 |
| `dump_types.txt` | dump 结果（游戏内部类型/方法/字段清单，查名字用） |
| `build.ps1` | 改完代码重新编译（**必须先关游戏**，否则 DLL 被占用） |
| `screenshots/screenshot.png` | 成品效果 |

### 挂钩总表

| 目标方法 | 用途 | 类型 |
|---|---|---|
| `Game.Update` | 无后座 + 自瞄（前缀，赶在输入/生成指令之前） | Prefix |
| `Match.TraceBullet` | 穿墙（拉满武器穿透力） | Prefix |
| `BulletPenetration.Remaining` | 穿墙（穿透余量→9999） | Postfix |
| `BallisticSurface.Named/.For` | 穿墙（材质穿透系数→999） | Postfix |
| `Weapons.SpreadRadius` | 无散射（散布→0） | Postfix |
| `ControlInput.Held` | 自动开火（测试用） | Postfix |

### 关键类

| 类 | 用途 |
|---|---|
| `Dustline.Game`（单例 `Instance`） | `Offline`/`Local`/`Camera`/`State`/`yaw`/`pitch` |
| `Dustline.Core.Snapshot` | `Players[]`（**字段**不是属性）+ `LocalId` |
| `Dustline.Core.Player` | `Position`/`Eye`/`Team`/`Bot`/`Alive`/`Health`/`Recoil`/`AccuracyPenalty`/`RecoilIndex` |
| `Dustline.Core.Weapons` | `ShotDirection`/`ShotDirections`/`SpreadRadius` |
| `Dustline.Core.BulletPenetration` | `Remaining(...)` |
| `Dustline.Core.BallisticSurface` | `PenetrationModifier` |
| `Dustline.Core.V3` | 三维向量（字段 X/Y/Z） |
| `Dustline.ControlInput` | `Held(GameAction)` |

### 视角角度约定（实测）

```
Aim(yaw=0,  pitch=0)  = (0, 0, 1)            yaw=0°  → +Z
Aim(yaw=90, pitch=0)  = (1, 0, 0)            yaw=90° → +X
Aim(yaw=0, pitch=+45) = (0, -0.707, 0.707)   pitch>0 → 朝下
```

**反解（度）：** `yaw = atan2(d.x, d.z)`，`pitch = atan2(-d.y, sqrt(d.x²+d.z²))`

### 屏幕角度换算（圈半径怎么来的）

```
焦距 focal = (屏幕高/2) / tan(相机竖直FOV/2)
半径 r     = focal × tan(角度)
```
1080p + FOV 58 → focal ≈ 974px，25° → 454px。

**但代码里不是用这个公式**，而是"把相机前向转 angle 度再投影一次"，
避免依赖 `fieldOfView` 的语义（水平还是竖直）。两者结果一致（已验证）。

### 踩过的坑

1. **`Snapshot.Players` 是字段不是属性** —— 用 `GetProperty` 拿到 null，ESP 一直不画
2. **`Command` 是结构体** —— Harmony 前缀改的是副本，所以子弹追踪做不了
3. **`Game.Update` 前缀 vs LateUpdate** —— 前缀赶在游戏读输入/生成指令之前，当前帧才受影响
4. **锁定角与画圈角必须同单位** —— 一个用 `AimFov`、一个用 `AimFov/2`，差 2 倍
5. **夹角和投影必须同原点** —— 用"眼睛夹角 + 相机投影"会得出假数据
6. **`Weapons` 是复数命名空间** —— `Dustline.Core.Weapons`
7. **Unity 6 过滤合成输入** —— 自动化测试点不动游戏菜单，所以加了「自动开局」「自动开火」

---

## 已知限制

- **窗口失焦时游戏自动暂停**（"单人对局已暂停"）—— 游戏机制，不是插件问题。
- **隔墙伤害衰减**是游戏机制，厚墙后本来就要多打几枪。
- 游戏大版本更新后类名/方法名可能变动，日志会打印"找不到 xxx"。

## 卸载

```
D:\cs\winhttp.dll
D:\cs\doorstop_config.ini
D:\cs\.doorstop_version
D:\cs\BepInEx\        （整个文件夹）
```
